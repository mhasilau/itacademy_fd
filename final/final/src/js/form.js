import { getToken } from '../api/api-hadlers';

// const submit = document.getElementById('submit');

// window.onload = () => {

//     if (window.location.pathname === './sign-in.html' && getToken()) {
//         window.location.href = './twits.html';
//     }